#include <iostream>
#include <map>
#include <vector>
using namespace std;

vector<int> rangos;
int nDatos;
int nRangos;

template <typename T>
map<T,int> construirMap(){
  cin >> nDatos >> nRangos;
  rangos = vector<int>(nRangos);
  map<T,int> datos;
  for(int i = 0; i < nRangos; ++i){
    cin >> rangos[i];
  }
  for(int i = 0; i < nDatos; ++i){
    T dato;
    cin >> dato;
    datos[dato]++;
  }
}

template <typename T>
void imprimirMins(const map<T,int>& datos){
  for(int i = 0; i < nRangos; ++i){
    int rango = rangos[i];
    typename map<T,int>::const_iterator it = datos.begin();
    int apariciones;
    while(it != datos.end() and rango > 0){
      apariciones = it->second;
      if(apariciones == 1){
	--rango;
	if(rango > 0) ++it;
      }
      else{
	for(int i = 0; i < apariciones; ++i) --rango;
	if(rango > 0) ++it;
      }
    }
    cout << it->first;
    if(i < nRangos-1) cout << " ";
  }
  cout << endl;
}

int main(){
  map<int,int> m;
  m = construirMap<int>();
  imprimirMins<int>(m);
}